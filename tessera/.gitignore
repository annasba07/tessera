__pycache__/
*.py[cod]
*.egg-info/
.venv/
.env
.env.local

# Build artifacts
dist/
build/

# Per-run outputs (keep cache, drop ad-hoc outputs).
# Anchored to repo root so it doesn't shadow src/tessera/narratives/.
/narratives/
synthesis.json
synthesis.md
observations.json
